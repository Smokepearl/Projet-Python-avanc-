requests>=2.31.0
matplotlib>=3.7.0
Pillow>=10.0.0
python-docx>=1.1.0
# Optionnel : permet à Python d'utiliser le magasin de certificats du système
# (utile derrière un proxy d'entreprise qui inspecte le trafic TLS).
truststore>=0.9.0
